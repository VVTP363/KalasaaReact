rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Vain kirjautuneet käyttäjät saavat lukea ja kirjoittaa
    match /_meta/{docId} {
  allow read: if true;
  allow write: if request.auth != null;
}

  }
}

